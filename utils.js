function showMessage(messageText, id) {
    document.getElementById(id).textContent = messageText;
}

function showMessage(messageText) {
    document.getElementById("message").textContent = messageText;
}